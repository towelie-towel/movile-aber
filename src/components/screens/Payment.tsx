import React from 'react';

import { Text, View } from '~/components/shared/Themed';

export default function PaymentScreen() {
    return (
        <View className={'w-full h-full p-5'}>
            <Text className={'text-lg text-center'}>Payment</Text>
        </View>
    );
} 